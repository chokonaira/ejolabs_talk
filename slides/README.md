# Slides

Use the clean deck:

```text
session-5-rag-clean.pptx
```

On macOS, from the `code/` folder, you can open it with:

```bash
open ../slides/session-5-rag-clean.pptx
```

That is the deck to use for the workshop.
